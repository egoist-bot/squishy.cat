{
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "poi": "^9.6.13",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.1"
  },
  "dependencies": {
    "vue-i18n": "^8.1.0",
    "vue-lazyload": "^1.1.4",
    "vue-resource": "^1.3.4"
  },
  "scripts": {
    "dev": "poi",
    "build": "poi build",
    "deploy": "npm run build && touch dist/CNAME && echo 'squishy.cat' >> dist/CNAME && gh-pages -d dist -b master"
  }
}
